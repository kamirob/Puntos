<%= form_with(model: category_equipment, local: true) do |form| %>
  <% if category_equipment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(category_equipment.errors.count, "error") %> prohibited this category_equipment from being saved:</h2>

      <ul>
      <% category_equipment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
